# kb-kit · 自成长知识库 —— 架构介绍与业务流程介绍

> 一键部署的自成长 Obsidian 知识库。核心不是一个「存笔记的仓库」，而是一条**数据管线 + 一个自改进闭环**：知识持续流入、被检索问答、再由真实反馈驱动自我优化，同时被治理与防塌机制约束，保证「越用越准、但不会自嗨到崩坏」。
>
> 配套图：`arch/kb-kit-pure-architecture.html`（组件/分区架构）与 `arch/kb-kit-pure-dataflow.html`（自上而下业务流程图，每点挂真实源码行号可溯源）。

---

## 一、架构介绍

### 1.1 一句话定位
**「外部真实信号驱动 + 有界可回滚改动 + 多道防塌闸」的自改进知识库。** 它把知识库当作一个会自我生长的系统（Recursive Self-Improvement），而不是静态文档集合。

### 1.2 分层拓扑（5 层 + 1 运行时底座）

```
┌─────────────────────────────────────────────────────────────┐
│  ① 输入层  Sources                                            │
│  用户/运营者 · DSH Agent 记忆 · Codex/Chat · 原始 raw/         │
├─────────────────────────────────────────────────────────────┤
│  ② 摄入 & ETL 层  ingest                                     │
│  归一 → 分块 → 分类 → 建索引  (memory_ingest / intake_triage / classify)
├─────────────────────────────────────────────────────────────┤
│  ③ 存储层  PARA vault (00收件箱 … 90归档)                      │
│  单一事实源；写前 git checkpoint、写后留回滚锚点                 │
├──────────────┬──────────────────┬───────────────────────────┤
│  ④ 检索消费层 │  ⑤ 自改进 & 治理层 │                             │
│  检索→精排→综合 │  外接阀 L0/L1/L2  │                             │
│  使用捕获      │  RSI T1/T2/T3    │  六维度健康·矛盾·演进·巡检       │
├──────────────┴──────────────────┴───────────────────────────┤
│  运行时底座  插件化 protocol → 拓扑排序分发 → kb CLI → cron       │
└─────────────────────────────────────────────────────────────┘
```

| 层 | 职责 | 代表模块 |
|---|---|---|
| ① 输入 | 四类源接入 | `memory_ingest_json` / `memory_ingest_sqlite` / `ingest_chat` / `memory_ingest`(mirror) |
| ② 摄入/ETL | 归一、语义分块、内容/域归类、收件箱 triage | `memory_ingest`、`intake_triage`、`classify` |
| ③ 存储 | PARA 结构化 vault、frontmatter 元数据 | `template-vault/`（00…90）、`📖-知识库管理方案.md` |
| ④ 检索消费 | TF-IDF 余弦检索→精排→综合，命中与使用信号捕获 | `kb_query`、`rag`、`rerank`、`kb_usage`、`graph` |
| ⑤ 自改进&治理 | RSI 三阶编排 + 外接阀 + 健康/矛盾/演进/巡检 | `kb_engine`、`kb_rsi`、`kb_embed/claim/fitness/usage`、`kb_contradiction`、`kb-healthcheck`、`evolution_log` |
| 运行时 | 插件协议、依赖拓扑排序分发、统一 CLI、定时编排 | `plugin_base`、`plugin_registry`、`kb_launcher`、`growth_cron.sh` |

### 1.3 三大核心机制

**① RSI 自改进引擎（T1 / T2 / T3）**
- **T1 自我整理**：去重合并（`MERGE_MIN_SIM=0.97`）、补链、`retire` 退役标记、收件箱归类。**收件箱只给建议、不自动路由**（保守防自闭）。
- **T2 自我加权**：把「外部接地的价值」转成 `importance`。命中 = 价值信号，只升不降、带上限、回写必 commit。
- **T3 有界调参**：有界调节 `confidence_floor` / `t3_step`，指标停滞（trend≤0）时收缩收紧。**默认关闭、需双保险**（`--run` 出现 AND 开关开）才跑，避免失控。

**② 开放 RSI 外接阀（L0 / L1 / L2）—— 防 Model Collapse 的关键**
闭环自喂会「自欺」（Synthetic Collapse），所以必须把**外部真实信号**引入适应度：
- **L0 语义地基**（`kb_embed`）：本地 vLLM embedding，为相似性/语义提供地基。
- **L1 声明验证**（`kb_claim`）：从知识里抽可证伪声明，新 raw 流入时 LLM 判「证实/证伪/无关」，把适应度锚向**预测误差**而非自抬。
- **L2 真实使用**（`kb_usage` + `kb_fitness`）：把再问率 / 查询成功率等真实使用代理纳入适应度。

**防塌外接闸**：只有 `freshness ≥ 5%` 且 `cross_ratio ≥ 15%`（即确有新异信息且观点够多元）时，才允许实质自我改进，否则打折。

**③ 四道防塌闸 + 写库必 checkpoint**
1. **外部接地闸** —— 必须有真实外部信号才动；
2. **适应度闸** —— 分数要经得起现实检验（L1/L2）；
3. **有界 + 回滚闸** —— 调参有界、改动 `git checkpoint` 可 `reset --hard`；
4. **多样性闸** —— 观点多元才允许收敛。

### 1.4 治理 & 运行时
- **治理**：六维度健康分（`kb_health`，权重配置化）、矛盾检测（`kb_contradiction`，lint 交人工）、演进日志（`evolution_log`，四类事件）、巡检八门禁（`kb-healthcheck`）。
- **运行时**：`plugin_base` 定义抽象协议 → `plugin_registry` 依赖拓扑排序分发 → `kb_launcher` 统一路由；`growth_cron.sh` 按节拍（成长 / 巡检 / 备份-演练）隔离单步，**全失败非 0 退出**供监控察觉。

---

## 二、自成长知识库 · 业务流程介绍

业务流程就是一条数据从流入到被消费、再到反哺自身优化的闭环。下图自上而下标了 5 个处理带（①→⑤）与两条反馈回路：

> 对照图：`arch/kb-kit-pure-dataflow.html`（每条连线、每个节点都挂真实源码行号，可点击溯源到 commit `4a54d62`）。

### 2.1 端到端主流程（数据从左/上流向右/下）

```
① 输入源 ──→ ② 摄入&ETL ──→ ③ PARA vault ──→ ④ 检索&问答 ──┐
(4类源)      归一/分块/分类      单一事实源        检索/精排/综合    │
     ↑                                            │    使用捕获        │
     └────── ⑤ 治理&运行时 (cron 触发摄入) ←── RSI 引擎 ←─┘    │
                     ↑                    │                     │
                     └───── 真实适应度锚 (外接阀 L0/L1/L2) ←─────┘
                              │
                              └───── T1/T2 改进写回 vault（虚回环）
```

- **① 输入**：用户投递收件箱/提问；DSH Agent 记忆流、Codex/Chat 对话、以及 `raw/` 原始流，四类源经 ETL 归一。
- **② 摄入/ETL**：`memory_ingest` 多源同步（sync/extract/mirror）→ `intake_triage` 收件箱 triage（**只建议**）→ `classify` 内容类型/域归类 → 语义分块 → 写入 vault。**写前 git checkpoint，写后留回滚锚点。**
- **③ PARA 存储**：`00收件箱 … 90归档`，frontmatter 元数据统一，单一事实源。
- **④ 检索&问答**：`rag` 的 TF-IDF 余弦（中文 unigram+bigram+synonyms）→ `rerank` 二次精排 → `kb_query` 综合出答案；`durability` 门达标则**提议**存为编译笔记，**人工 apply 入库**（人在环）。命中与窗口内再问率经 `_record_hits` 回写 `feedback_state`，反哺适应度。
- **⑤ 自改进&治理**：外接阀把真实信号喂入适应度 → `kb_engine` 跑 T1/T2/T3 → 治理记录事件、巡检门禁 → `cron` 节拍回灌输入与改进。

### 2.2 两条闭环（自成长的引擎）

**闭环 A —— 开放 RSI 自改进（核心）**
```
vault 知识 ─抽取→ L0 语义 ─声明→ L1 证伪 ─使用→ L2 代理 ─适应度→ RSI(T1/T2/T3) ─写回→ vault
        ↑                                                                                    │
        └──────────────────── 新鲜度/多样性 双闸（不满足则打折）────────────────────────────┘
```
真实信号（embedding/LLM 判定/再问率）经 L0/L1/L2 外接阀把「适应度锚向现实而非自抬」，驱动 T1/T2/T3 把改进**写回 vault**，形成自生长。四道闸保证只在「有新异 + 够多元 + 有界 + 可回滚」时才动。

**闭环 B —— 治理 & 运行时节拍**
```
RSI 事件 → 演进日志 → 健康/矛盾巡检 → 门禁节拍 → cron ─触发→ 摄入 / 自改进（回灌 ①②⑤）
```

### 2.3 两条典型业务路径（端到端发生了什么）

**路径 1：用户提一个问题**
```
用户提问
  → kb_query.query() 入口（人在环）
  → vault 语义检索 (rag 余弦) → rerank 精排 → 综合出答案
  → _record_hits 记录命中 + 窗口内再问率  → feedback_state（为 L2 适应度攒信号）
```

**路径 2：一篇新笔记流入后的自改进**
```
新 raw 流入 (①②)
  → L1 抽取可证伪声明 → 用新料判定「证实/证伪」(kb_claim)
  → freshness / cross_ratio 双闸判定：允许实质改进？
       ├─ 是 → RSI：T1 去重合并/补链、T2 按命中加权、(T3 可选调参)
       └─ 否 → 打折/不动，避免自闭改进
  → 写前 git checkpoint，写后留回滚锚点 → evolution_log 追加事件 → 治理巡检
```

### 2.4 为什么「自成长但不易崩」
| 风险 | 机制 |
|---|---|
| 闭环自喂 → Model / Synthetic Collapse | 开放外接阀 L0/L1/L2，适应度锚向现实 |
| 自我改进失控 | 双闸（新鲜度+多样性）+ T3 默认关 + 有界调参 |
| 错误改动无法挽回 | 每次写库必 git checkpoint，可 reset --hard |
| 收敛成信息茧房 | 多样性闸 + 矛盾检测交人工 |
| 检索答非所问 | durability 门 + 人工 apply，人在环 |

---

## 三、如何延伸查看
- **架构（组件/分区）**：打开 `arch/kb-kit-pure-architecture.html`，深/浅主题、可搜索、可导出 PNG/SVG。
- **业务流程（带源码溯源）**：打开 `arch/kb-kit-pure-dataflow.html`，每个节点/连线点选即可跳转到真实代码行（commit `4a54d62`）。
- **源码根**：`template-vault/pipeline/`（46 个 .py，含 RSI 引擎与插件）。
